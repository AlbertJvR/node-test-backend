{
  "name": "node-test-backend",
  "version": "1.0.0",
  "description": "A backend used to test / build frontend apps",
  "main": "./src/server.ts",
  "scripts": {
    "start": "rm -rf ./dist && tsc && node dist/server.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Albert",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.7",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^6.7.0"
  }
}
